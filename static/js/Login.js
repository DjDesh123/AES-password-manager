// this function will be taking the user credentials and then checking if they are a part of the database that needs to be implemented via json file
function CheckCredentials(username,password){
    console.log("external js working ");
    //gets the username and password
    var userInput1 = document.getElementById(username).value;
    var userInput2 = document.getElementById(password).value;

    // need to make the post thing to send it back to app so then i can check the credentials of the log in through anotehr files database like planned
}